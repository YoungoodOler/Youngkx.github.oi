---
title: "文章标题"
date: "2026-07-23"
slug: "article-slug"
tags:
  - AI
# 可选：code / cs / ai / vlog / talk / web / network / timeline
card: ai
excerpt: "用一两句话概括文章内容，这段文字会显示在主页卡片和文章列表中。"
draft: true
---

## 第一节

从这里开始写正文。支持标准 Markdown、表格、引用、链接和代码块。

```ts
const message = 'Hello, Youngkx';
console.log(message);
```

## 第二节

发布前请完成以下操作：

1. 把本文件复制为新的 `.md` 文件，文件名不要以下划线开头；
2. 修改标题、日期、slug、标签、卡片样式和摘要；
3. 删除 `draft: true`，或改成 `draft: false`；
4. 运行 `npm.cmd run check` 和 `npm.cmd run build`。
