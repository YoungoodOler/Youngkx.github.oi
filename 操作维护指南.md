# Youngkx 博客操作与维护指南

> 适用项目：Youngkx Blog
> 本地目录：`D:\youngkx`
> 正式地址：<https://www.youngkx.cn>
> GitHub 仓库：<https://github.com/YoungoodOler/Youngkx.github.oi>
> Cloudflare Worker：`youngkxblog`
> 最后整理：2026 年 8 月 16 日

本文档用于日常维护 Youngkx 博客。即使不熟悉 Next.js 或 Cloudflare，也可以按照对应章节逐步操作。

---

## 1. 当前网站结构

网站目前使用以下技术：

- Next.js：页面、路由和静态构建；
- React：页面组件；
- Framer Motion：滚动、卡片和过渡动画；
- Three.js：主页背景星球与交互粒子场；
- Cloudflare Workers：正式部署、静态文件托管、Links API 和域名重定向；
- Cloudflare KV：保存 Links 目录的分组、网址和说明；
- GitHub：保存源代码和版本历史。

访问关系如下：

```text
访问 youngkx.cn
        ↓ 308 永久重定向
访问 www.youngkx.cn
        ↓
Cloudflare Worker youngkxblog
        ↓
读取 Next.js 构建生成的 out/ 静态文件
```

GitHub Pages 已停用。仓库中不再包含旧 Hexo 主题、GitHub Pages 工作流、`CNAME`、`.nojekyll` 或旧静态服务器脚本。

---

## 2. 重要目录和文件

```text
D:\youngkx\
├─ app/
│  ├─ page.tsx                    # 主页入口
│  ├─ layout.tsx                  # 全站标题、描述和网站图标
│  ├─ globals.css                 # 全站样式、亮暗主题、响应式布局
│  ├─ icon.png                    # 浏览器标签图标，512 × 512
│  ├─ apple-icon.png              # Apple 设备图标，180 × 180
│  ├─ articles/page.tsx           # 所有文章页入口
│  ├─ categories/page.tsx         # 分类页入口
│  ├─ links/page.tsx              # 常用链接公开目录
│  ├─ links/manage/page.tsx       # Links 可视化管理入口
│  └─ [year]/[...slug]/page.tsx   # 支持任意年份的文章动态路由
├─ components/
│  ├─ HomePage.tsx                # 主页内容、滚动标题层、导航和文章卡片
│  ├─ ArticlesPage.tsx            # 所有文章列表
│  ├─ CategoriesPage.tsx          # 分类内容
│  ├─ LinksPage.tsx               # 从 API 读取并展示常用链接
│  ├─ LinksManager.tsx            # Links 新增、排序、导入导出和保存界面
│  ├─ ArticleView.tsx             # 文章正文界面和快捷工具
│  ├─ Scene.tsx                   # 全站 Three.js 粒子背景、主页星球与指针交互
│  ├─ SiteExperience.tsx          # 全站主题状态、粒子过渡和站内跳转
│  └─ SiteFooter.tsx              # 运行时间和最近更新时间
├─ content/
│  ├─ posts/                      # 新文章 Markdown 文件和模板
│  └─ articles.json               # 原有文章兼容数据
├─ cloudflare/
│  ├─ worker.ts                   # 域名重定向、静态资源、Links API 和鉴权
│  ├─ worker-configuration.d.ts   # Wrangler 自动生成的 Worker 类型
│  └─ tsconfig.json               # Worker 独立类型检查配置
├─ config/
│  ├─ playwright.config.ts        # 桌面与手机端浏览器回归配置
│  └─ vitest.config.mts           # 单元测试配置
├─ lib/
│  ├─ articles.ts                 # 自动排序、统计、分类和链接生成
│  ├─ article-selection.ts        # 为最大动态卡片选择最新文章
│  ├─ markdown-articles.ts        # Markdown 读取与渲染
│  ├─ useful-links.ts             # Links 数据结构、限制和安全校验
│  └─ card-presets.ts             # 动态卡片类型与自动匹配规则
├─ 操作维护指南.md                # 本维护手册
├─ public/avatar.webp             # 导航栏和主页头像
├─ scripts/                       # 性能预算和缓存清理脚本
├─ tests/                         # 单元测试、Worker 测试和浏览器回归
├─ wrangler.jsonc                 # Cloudflare Worker 配置
├─ next.config.mjs                # Next.js 静态导出配置
├─ package.json                   # 依赖和常用命令
├─ CHANGELOG.md                   # 主要功能、架构和发布记录
└─ README.md                      # 项目简介
```

以下目录由工具自动生成，不需要手工修改，也不要提交到 GitHub：

- `node_modules/`：项目依赖；
- `.next/`：Next.js 构建缓存；
- `out/`：最终静态站点；
- `.wrangler/`：Cloudflare 本地缓存。

运行 `npm.cmd run clean` 可以一次清理这些构建缓存、部署产物和测试报告，不会删除源码、文章或 `node_modules/`。

这些目录可以在排查构建问题时删除，之后重新安装或构建即可生成。

---

## 3. 第一次配置电脑

### 3.1 安装必需软件

需要安装：

1. Node.js 22；仓库中的 `.node-version` 是唯一版本基准；
2. Git；
3. 一个代码编辑器，例如 Visual Studio Code；
4. 可访问 GitHub 和 Cloudflare 的网络环境。

检查是否安装成功：

```powershell
node --version
npm.cmd --version
git --version
```

### 3.2 获取项目

```powershell
git clone git@github.com:YoungoodOler/Youngkx.github.oi.git D:\youngkx
Set-Location D:\youngkx
npm.cmd install
```

如果使用 HTTPS：

```powershell
git clone https://github.com/YoungoodOler/Youngkx.github.oi.git D:\youngkx
```

### 3.3 登录 Cloudflare

第一次部署前运行：

```powershell
npx.cmd wrangler login
```

浏览器会打开 Cloudflare 授权页面。确认授权后回到终端即可。

不要把 Cloudflare Token、GitHub Token、账号密码或 `.env` 文件提交到仓库。

---

## 4. 本地启动网站

进入新的项目根目录后运行：

```powershell
Set-Location D:\youngkx
npm.cmd install
npm.cmd run dev
```

浏览器打开：

```text
http://localhost:3000
```

停止本地服务：在终端按 `Ctrl + C`。

如果 PowerShell 提示“禁止运行脚本”，使用 `npm.cmd`，不要使用 `npm`。

如果 3000 端口被占用，Next.js 通常会自动改用 3001。请以终端显示的地址为准。

---

## 5. 最常用的维护流程

每次修改建议按照以下顺序：

```powershell
# 1. 获取远端最新代码
git pull --ff-only

# 2. 启动本地预览
npm.cmd run dev

# 3. 修改并在浏览器检查

# 4. 执行完整验证
npm.cmd run format:check
npm.cmd run validate

# 5. 检查桌面和手机端关键交互
npm.cmd run test:e2e

# 6. 查看将要提交的文件
git status
git diff

# 7. 提交到 GitHub；推送 main 后会触发 Cloudflare 自动部署
git add 文件名
git commit -m "简要说明本次修改"
git push

# 8. 需要手工发布时先预检，再部署到 Cloudflare
npm.cmd run deploy:dry
npm.cmd run deploy
```

`npm.cmd run validate` 会依次执行网站和 Worker 类型检查、ESLint、Vitest、生产构建与性能预算。`npm.cmd run test:e2e` 会启动本地 Worker 预览，并在 1440px 桌面端和 390px 手机端检查关键交互。

推送或创建 Pull Request 后，GitHub Actions 会在 Node.js 22 环境中重复依赖审计、Worker 类型同步、格式检查、完整验证和 Playwright 浏览器回归。当前 `main` 分支还连接了 Cloudflare 自动部署：推送后会生成新的 Worker 版本并接管正式流量。`npm.cmd run deploy` 仍可用于手工发布，但不要在自动构建仍进行时反复部署；如果两种方式同时执行，以 `wrangler deployments list` 显示的最新 100% 流量版本为准。

---

## 6. 修改主页内容

主页主要内容位于：

```text
components/HomePage.tsx
```

### 6.1 修改首页轮播句子

找到：

```ts
const introQuotes = ['Life is real, life is earnest.'];
```

可以修改文字，也可以继续增加句子。存在两句及以上时才会轮播，轮播间隔目前是 4200 毫秒，可在同一文件搜索 `4200` 调整。

### 6.2 修改主页展示文章

主页文章不再单独维护。它们会从 `content/posts/*.md` 和原有的 `content/articles.json` 自动读取，并按照发布日期从新到旧排列。

动态索引区第一张最大的 `Latest Signal` 卡片会调用 `lib/article-selection.ts`，从全部文章中重新比较 `date` 并选出最新文章，不依赖数组原有顺序。新增文章并重新构建后，卡片的标题、摘要、日期、分类和链接会自动更新；卡面星图和交互样式保持不变，不会因为文章类型重新生成视觉效果。

主页默认只显示前三篇：

```ts
posts.slice(0, 3);
```

要更换主页前三篇或最大卡片，应修改文章的 `date`。文章编号、日期格式、链接、颜色和普通文章卡片由程序自动生成。Markdown 顶部的 `card` 字段可以主动选择下方普通文章卡片的动画，但不影响最大的 `Latest Signal` 卡面。

---

## 7. 修改或新增文章

### 7.1 推荐方式：使用 Markdown

新文章统一放在 `content/posts/`。先复制 `content/posts/_template.md`，再把副本改成普通文件名，例如 `my-ai-note.md`。以下划线开头的模板不会发布。

每篇文章顶部必须带有 YAML 信息：

```markdown
---
title: '我的 AI 笔记'
date: '2026-07-23'
slug: 'my-ai-note'
tags:
  - AI
  - 随笔
card: ai
excerpt: '这是一篇用于说明 Markdown 发布流程的文章。'
draft: false
---

## 正文标题

从这里开始使用 Markdown 编写正文。
```

字段说明：

- `title`：文章标题，必填；
- `date`：发布日期，必须写成带引号的 `"YYYY-MM-DD"`；
- `slug`：网址最后一段，不能包含 `/`、`\`、`?` 或 `#`；
- `tags`：至少一个标签，一篇文章可以属于多个分类；
- `card`：主页动态卡片预设，可选；
- `excerpt`：主页和文章列表显示的摘要，必填；
- `draft`：为 `true` 时不发布，改为 `false` 或删除这一行后发布。

系统会根据日期和 slug 自动生成网址，例如 `/2026/07/23/my-ai-note/`。

### 7.2 选择动态卡片

| card 值    | 视觉与用途               | 推荐文章                     |
| ---------- | ------------------------ | ---------------------------- |
| `code`     | 动态终端、扫描线与光标   | C/C++、代码教程、编程笔记    |
| `cs`       | 芯片、逻辑线路与信号流   | CS 基础、系统、数据结构      |
| `ai`       | 神经网络节点与流动脉冲   | AI、机器学习、LLM            |
| `vlog`     | 取景框、风景层次与播放轴 | Vlog、摄影、旅行、生活       |
| `talk`     | 对话气泡与输入动画       | 闲谈、随笔、日记、想法       |
| `web`      | Client/Server 请求流     | Web、HTTP、前端              |
| `network`  | 图节点与连线流动         | 图论、网络、算法             |
| `timeline` | 时间轴与阶段信号         | 经历总结、比赛记录、成长记录 |

在 Markdown 顶部写入例如 `card: vlog` 即可选择。如果不写 `card`，系统会根据 `tags` 自动匹配：`AI` 匹配 AI 卡片，`VLOG` 或 `生活` 匹配影像卡片，`闲谈` 或 `随笔` 匹配对话卡片，`WEB` 匹配请求流卡片。

### 7.3 原有 JSON 文章

原有正文继续保存在：

```text
content/articles.json
```

每篇文章结构如下：

```json
{
  "path": ["2026", "07", "21", "example"],
  "title": "示例文章",
  "date": "2026-07-21",
  "tags": ["WEB", "随笔"],
  "excerpt": "文章摘要",
  "content": "<h2 id=\"section\">正文标题</h2><p>正文内容</p>"
}
```

字段说明：

- `path`：完整年份、月份、日期和文章别名，同时决定文章网址；
- `title`：文章标题；
- `date`：发布日期，格式为 `YYYY-MM-DD`；
- `tags`：标签数组，一篇文章可以属于多个分类；
- `excerpt`：SEO 描述和列表摘要；
- `content`：HTML 格式正文。

### 7.4 修改现有文章

1. 新 Markdown 文章在 `content/posts/` 找到对应文件；原有文章仍在 `content/articles.json`；
2. 修改 `title`、`date`、`tags`、`excerpt` 或 `content`；
3. 运行 `npm.cmd run check`；
4. 运行 `npm.cmd run build`；
5. 打开文章页检查标题、目录、代码块和链接。

### 7.5 新增文章

新文章只需复制 Markdown 模板、填写顶部信息并撰写正文。系统会自动完成：

- 按发布日期排序；
- 生成文章编号；
- 生成文章网址；
- 更新主页前三篇；
- 更新“查看全部 N 篇”；
- 更新所有文章页的总数和大号数字；
- 根据 `tags` 创建分类；
- 更新主题总数；
- 更新每个分类的文章数量和文章列表；
- 为新年份生成文章页面。

新增后仍必须运行类型检查和生产构建。文章数据缺少路径、标题、日期、标签或正文时，构建会直接报错。

### 7.6 新年份文章

当前通用文章路由是：

```text
app/[year]/[...slug]/page.tsx
```

因此新增任意年份的文章都不需要再创建路由。例如：

```json
"path": ["2027", "01", "03", "new-post"]
```

会自动生成：

```text
/2027/01/03/new-post/
```

### 7.7 编辑原有 JSON 的注意事项

- 每个对象之间必须有英文逗号；
- JSON 字符串必须使用英文双引号；
- 正文中的双引号要写成 `\"`；
- 不要在最后一个对象后留下多余逗号；
- 中文文件必须保存为 UTF-8；
- 不要直接使用 Word 编辑 JSON。

修改后可运行以下命令检查 JSON 是否有效：

```powershell
node -e "JSON.parse(require('fs').readFileSync('content/articles.json','utf8')); console.log('文章 JSON 正常')"
```

### 7.8 代码块语言与复制

文章详情页会自动为 Markdown 围栏代码块和历史 HTML 代码块添加工具栏，工具栏显示语言名称和“复制”按钮。复制内容只包含代码正文，不包含工具栏或历史代码块的行号。

Markdown 文章应优先在三个反引号后明确填写语言，例如：

````markdown
```ts
const message: string = 'Hello, Youngkx';
console.log(message);
```
````

明确填写的语言标记优先级最高。未填写时，`ArticleView.tsx` 会根据类名和代码特征识别常见的 C、C++、C#、JavaScript、TypeScript、Python、Java、Rust、Go、HTML、CSS、JSON、Shell、PowerShell、SQL、YAML、Markdown 和 HTTP；无法可靠判断时显示 `Plain text`。

修改代码块增强功能时需要同时验证：

- Markdown 新文章的 `<pre><code class="language-*">` 结构；
- `content/articles.json` 中的历史 `<figure class="highlight ...">` 结构；
- 复制后的换行、缩进和特殊字符；
- 亮色、暗色和手机窄屏下的工具栏布局；
- 浏览器拒绝 Clipboard API 时的兼容复制路径。

---

## 8. 修改分类

分类由文章的 `tags` 自动生成。例如：

```json
"tags": ["OI", "算法"]
```

这篇文章会同时出现在 `OI` 和 `算法` 分类中。首次使用的新标签会自动出现在主页分类区和分类页，分类编号、锚点及文章数量也会自动生成。

`OI`、`C`、`WEB` 已在 `lib/articles.ts` 中配置了专用显示名称和说明。未知标签会直接使用标签文字作为标题，并显示“标签名 相关文章”。如果需要给新标签编写更正式的描述，可在 `lib/articles.ts` 的 `categoryDetails` 中增加可选配置；不配置也不会影响自动分类。

---

## 9. 修改头像和浏览器图标

当前有三份相关图片：

```text
public/avatar.webp    # 页面左上角头像
app/icon.png          # 浏览器标签图标
app/apple-icon.png    # Apple 设备图标
```

更换头像时建议同时替换三份文件，否则网页头像和浏览器图标可能不一致。

建议尺寸：

- `avatar.webp`：正方形，建议至少 512 × 512；
- `icon.png`：512 × 512；
- `apple-icon.png`：180 × 180。

文件名和路径不要改变。替换后重新执行：

```powershell
npm.cmd run build
npx.cmd wrangler deploy
```

浏览器会长期缓存 favicon。如果部署后仍显示旧图标，可以：

1. 使用无痕窗口访问；
2. 强制刷新页面；
3. 清除该网站的缓存；
4. 等待浏览器重新获取图标。

---

## 10. 修改标题、SEO 和页脚信息

### 10.1 全站标题和描述

修改：

```text
app/layout.tsx
```

主要字段：

```ts
export const metadata = {
  title: 'Youngkx',
  description: '记录 OI、C/C++ 与 Web 学习笔记。',
};
```

文章列表页和分类页还有各自的标题，分别位于：

- `app/articles/page.tsx`；
- `app/categories/page.tsx`。

文章详情标题会根据 `content/articles.json` 自动生成。

规范网址、Open Graph、站点地图和爬虫规则分别由页面 metadata、`app/sitemap.ts` 与 `app/robots.ts` 生成。新增文章会自动进入 `sitemap.xml`，不要手工编辑 `out/sitemap.xml` 或 `out/robots.txt`。

### 10.2 网站运行时间

修改：

```text
components/SiteFooter.tsx
```

`launchedAt` 是网站开始运行的时间。不要因为普通更新而修改它。

### 10.3 最近更新时间

`SiteFooter.tsx` 中的“最近更新”目前是手工文字。每次正式发布内容或界面后，应同步修改日期。

---

## 11. 修改动画、全站星空和样式

### 11.1 全站星空背景与主页星球

文件：

```text
components/Scene.tsx
```

这里控制全站 Three.js 背景粒子、指针光尘和点击反馈，以及仅在主页显示的星球、光线和随滚动变化的位置。

当前交互包括：

- 背景粒子自动漂浮、呼吸闪烁并随页面滚动改变位置；
- 粒子场由 `SiteExperience.tsx` 作为全站固定背景挂载；主页显示星球，文章、分类、正文和其他页面只显示粒子；
- 鼠标靠近时，附近粒子形成轻微涡流并增强亮度；
- 桌面端移动鼠标时产生短暂光尘轨迹；
- 桌面端点击时只产生少量光尘和轻微粒子扰动，不再显示大尺寸扩散环；
- 主页星球在首页生命周期内保持挂载，滚动进出只调整位置、缩放和透明度，不要使用接近零的可见阈值反复启停对象；
- 进入文章区域时自动降低粒子亮度，避免干扰阅读。

粒子色板应保持接近真实星空：以冷白、蓝白和钢蓝为主，只使用少量暖白表现恒星色温差异。不要加入高饱和紫色、绿色、粉色或彩虹色粒子。

桌面端和手机端使用不同的粒子数量、几何细分与设备像素比。手机端默认关闭触摸轨迹和点击反馈，但保留完整动画帧率。不要只在大屏设备上测试，也不要一次大幅提高粒子数量、几何细分或设备像素比，否则可能增加手机 GPU 负担。

手机端不要注册仅供桌面鼠标使用的轨迹、点击圆环和命中转换事件，也不要在渲染循环中继续更新不可见的交互对象。星球的球体、带状体和菱形体法线应在初始化时预计算，滚动变形时对位置与法线一起插值，避免每帧调用 `computeVertexNormals()`。

修改后重点测试：

- 首页首屏；
- 鼠标移动、点击和触摸交互；
- 文章区域文字是否仍清晰；
- 分类区域；
- 手机端性能；
- 亮色和暗色主题；
- 系统开启“减少动态效果”后的静态表现。

### 11.2 页面动画

主页 Framer Motion 动画主要位于 `components/HomePage.tsx`。首屏使用一个高于视口的滚动舞台和固定在视口内的内容层；当前桌面舞台为 `480svh`，平板和手机为 `450svh`：

- 页面初始状态只显示大号 `Youngkx Blog` 标题和背景星尘，不显示大星球；
- 用户向下滚动时，同一个标题根据首屏局部滚动进度保持清晰地缩小并迁移到第二阶段左侧，不对 `Youngkx Blog` 标题添加景深模糊；
- 第二阶段标题的横向位置根据标题实际宽度和简介内容栏左边缘动态计算，调整字号或缩放时仍需保持两者左对齐；
- `Youngkx` 与 `Blog` 使用独立标题层；第二阶段退出时，`Blog` 从下方向上压叠至 `Youngkx` 下半部但不完全重合，随后标题整体透明淡出；
- 星球从屏幕边缘沿弧线旋转飞入，引句、说明和操作入口作为独立层沿较长滚动区间从下方错峰淡入上浮；
- 引句内部轮播使用从左向右的横向扫入和滑出，以区别于外层文字组的纵向景深动画；
- 引句、说明和操作入口全部进入后会在加长的滚动舞台中完整同屏停留；它们与 `Youngkx Blog` 使用相同的末段滚动区间同步向上淡出，退出阶段不重新添加景深模糊；
- 星球进入文章区的横向移动与缩放必须根据首屏局部滚动进度在第二阶段末段触发，不要直接随全页滚动比例提前转场；文字全部就位时，桌面端星球应保持放大并占据右半屏；
- 手机端第二阶段标题使用独立的放大比例；修改比例后必须同步更新标题横向定位计算中的 `restingScale`，确保它仍与下方文字左对齐；
- 文章、分类和内页的大标题及主要内容块同样采用由模糊到清晰的景深浮入，不增加文字外框或遮盖板；
- 首屏末段与 `Cards` 动态索引区使用同一段局部滚动进度衔接：首屏文字提前淡出，卡片标题、透明度、纵向位移、`rotateX`、`scaleX` 和 `scaleY` 分散在约 1.5 个视口的滚动行程内逐步完成，不要把关键帧重新挤到末段或改回两个互不关联的入场动画；
- 四张卡片的动画包装层必须保持 `overflow: visible`、透明、无边框、无阴影，并且不得使用 `clip-path`、遮罩或 `contain: paint`；单张卡片自身的背景、边框、阴影和鼠标 3D 倾斜继续保留，卡片抬起后应能越过网格边界显示；
- 从首屏快速向下滚动或点击顶栏 `Cards` 时，`HomePage.tsx` 会根据首屏滚动舞台的稳定几何尺寸落到真实结束点，完整展示动态索引卡片，下一次滚动才进入 Articles；不要让浏览器按仍带动画位移的卡片容器计算原生锚点，也不要将其设为 CSS Scroll Snap 目标，否则展开位移归零后会产生约一个动画偏移量的错位；带 `#interactive-cards` 刷新或通过历史记录返回时，应使用 `signal-deck--settled` 完成态清除旧的 MotionValue 变换；
- “文章”区使用负外边距抵消自身顶部内边距和标题的初始上浮位移，并在接触视口时立即触发；滚动舞台淡出结束后应直接看到文章标题从下方接续浮入，不保留额外滚动空档；
- 整个过程由用户滚动驱动，不使用定时器自动播放，也不使用全屏遮罩。

全站主题和页面过渡位于 `components/SiteExperience.tsx`：

- 主题切换时由独立全屏 Canvas 复制真实星空色板，高密度星尘沿随机非线性曲线高速乱飞并形成短暂遮盖，在遮盖最完整时更新主题色；
- 手机端主题粒子幕应在约 0.9 秒内结束，并在粒子遮盖下直接更新页面颜色；不要同时对整页背景和文字颜色进行逐帧插值，否则会产生大面积重复重绘；
- 扰流遮盖使用比常驻背景更高的临时粒子密度、更大的曲线偏移和更长拖尾，但不得直接修改常驻 Three.js 粒子的数量与运动参数；
- 遮盖完成后，切换层星尘沿新的曲线继续逸散并淡出，显露已经更新的界面；
- 切换层与 `Scene.tsx` 中的 Three.js 背景粒子互相独立，不得为了切换动画修改、暂停或重建原有背景粒子场；
- Three.js 场景通过颜色插值跟随主题，不因切换主题重新初始化；
- 跨页面的站内链接复用同一套星尘扰流遮盖；普通同页锚点保持直接滚动，顶栏 `Cards` 入口则使用首屏稳定结束点校正动画容器的落点；
- 开启“减少动态效果”时，主题和页面均立即切换，不播放粒子幕。

调整时需要同时检查：

- 页面刚打开时标题是否完整位于视口中心；
- 首次滚动时标题是否平滑退出，没有突然跳动；
- 简介与标题的显示区间是否自然衔接；
- 顶栏 `Cards` 是否落在卡片动画真实结束点，标题与导航之间是否保留约 `48–68px` 空隙，字号是否与 Articles、Topics 一致；
- 单次高速下滑是否先停在完整的四卡片动态索引区，而不是直接跳到 Articles；
- 四张卡片的整体旋转展开是否保留，鼠标移入后卡片翘起部分是否在亮色和暗色主题下都没有被外层容器裁切；
- 四张卡片之间和四周是否没有额外的大底板、外层边框、背景色或包装层阴影；
- 文章区域是否在滚动舞台结束后正常出现；
- 亮暗主题下标题描边和正文对比度是否足够；
- 手机竖屏下大标题是否被裁切；
- `prefers-reduced-motion` 开启时标题和简介是否直接按普通文档流显示。

常见参数：

- `duration`：动画持续时间；
- `delay`：延迟；
- `stiffness`：弹簧响应速度；
- `damping`：弹簧阻尼；
- `whileInView`：滚动到元素时触发。

不要一次把弹簧参数调得过大，否则页面会抖动或消耗更多性能。

### 11.3 颜色、字体和响应式布局

文件：

```text
app/globals.css
```

导航、按钮、标签和元信息使用 `app/layout.tsx` 中通过 `next/font` 加载的现代粗体界面字体；主页、文章页、分类页、Links 页的大标题继续使用原有展示衬线字体。英文界面文字使用正常的首字母大写，不通过 CSS 强制转为全大写；`OI`、`URL`、`JSON` 等缩写可以保留大写。

修改后至少检查：

- 1440px 桌面宽度；
- 768px 平板宽度；
- 390px 手机宽度；
- 亮色主题；
- 暗色主题；
- 导航菜单打开状态；
- 长标题和中文换行。

手机端主页导航应保持为右上角小型悬浮框，不覆盖整个视口；需要验证菜单能够快速展开、链接触控区域不小于 44px，并支持点击外部或按 `Esc` 关闭。

### 11.4 性能约束与自动检查

- `components/Scene.tsx` 保持按需加载；不要把 Three.js 重新放入所有页面的同步首屏代码。
- Framer Motion DOM 动画统一使用 `LazyMotion` 和 `m` 组件；新增页面不要直接引入完整动画功能包。
- 常驻粒子数量和动态强度不以性能优化为由降低；优先减少重复计算、无效绘制和后台标签页渲染。
- 主题与页面过渡粒子应优先使用类型化数组、预计算波形与轨迹参数、按颜色批量绘制和适合手机的 Canvas 像素比；不得通过在遮盖期间继续对整页颜色做渐变来制造双重动画。
- 切换亮暗主题时只更新材质混合状态，不要仅因 blending 变化设置 `material.needsUpdate`，否则可能触发不必要的着色器重新编译。
- 手机窄屏避免对固定导航、大面积环境光和整张内容卡片使用实时 `blur` 或 `backdrop-filter`；优先使用径向渐变、高不透明背景、淡入位移和 `content-visibility` 降低合成与屏外布局开销。
- 滚动事件中不要反复查询 DOM 或读取 `scrollHeight`、`offsetHeight`、`getBoundingClientRect()` 等布局尺寸；页面总距离与动画舞台距离应在初始化和 `ResizeObserver` 通知布局变化时缓存，滚动过程中只计算进度。
- 字体由 `next/font` 构建时自托管，不要恢复 `fonts.googleapis.com` 或 `fonts.gstatic.com` 浏览器外链。
- `scripts/check-performance-budget.mjs` 会检查首页、列表页、文章页初始脚本以及 CSS、字体体积；确有必要提高预算时，应先说明新增体积来自哪项可见功能。
- `tests/e2e/site.spec.ts` 覆盖桌面和手机关键体验。调整首页第二阶段、主题切换、页面粒子幕、代码块或移动导航后，必须同步运行浏览器回归。

---

## 12. Links 目录与可视化管理器

主页的 `Useful Links` 卡片会进入独立的公开目录：

```text
https://www.youngkx.cn/links/
```

新增、修改和导入链接使用管理页：

```text
https://www.youngkx.cn/links/manage/
```

公开目录不需要登录；管理页的读取与写入由 `LINKS_ADMIN_TOKEN` 保护。数据保存在 Cloudflare KV 的 `LINKS` 绑定中，使用的键为 `directory:v1`。

### 12.1 本地打开管理页

普通的 `npm.cmd run dev` 只启动 Next.js，不包含 Cloudflare Worker API。需要测试 Links 管理功能时使用：

```powershell
npm.cmd run build
npm.cmd run preview:test
```

浏览器打开：

```text
http://127.0.0.1:8787/links/manage/
```

`preview:test` 会给本地 Worker 注入临时的 `LINKS_LOCAL_PREVIEW=true`，因此本地管理页自动解锁。这个标记只能存在于 `package.json` 的本地预览命令中，不要把它写入 `wrangler.jsonc` 或 Cloudflare 正式环境。

本地 KV 和线上 KV 互相独立。在本地添加的内容只用于测试，不会自动同步到正式网站；需要修改线上目录时，应直接进入正式管理页并保存。

### 12.2 首次设置或更换线上管理 Token

部署前确认已经登录正确的 Cloudflare 账号，然后运行：

```powershell
npm.cmd exec wrangler -- whoami
npm.cmd run links:secret
```

第二条命令会要求输入新的管理 Token，输入过程不会回显。建议使用密码管理器生成并保存至少 32 字符的随机 Token。

注意：

- Secret 名称固定为 `LINKS_ADMIN_TOKEN`；
- 不要把 Token 写入源码、`wrangler.jsonc`、文档、截图或 Git 提交；
- 不要把 Token 作为命令行参数传递；
- 再次执行 `npm.cmd run links:secret` 会替换旧 Token，旧 Token 会立即失效；
- 可以用 `npm.cmd exec wrangler -- secret list` 检查 Secret 名称是否存在，但该命令不会显示 Secret 内容。

### 12.3 新增一个链接

1. 打开 `/links/manage/`；
2. 线上环境输入 `LINKS_ADMIN_TOKEN`，本地预览会自动解锁；
3. 没有分组时选择 `Create Your First Group`，已有内容时选择 `Add Another Group`；
4. 填写 `Group Title` 和 `Group Description`；
5. 选择 `Add Link To ...`；
6. 填写链接字段；
7. 选择右上角 `Save Changes`；
8. 看到保存成功提示后，选择 `View Page` 检查公开目录。

每个链接包含：

- `Title`：公开页面显示的链接名称；
- `URL`：必须是完整的 `http://` 或 `https://` 地址；
- `Short Label`：简短类型，例如 `Tool`、`Service`、`Reading`；
- `Description`：说明这个链接的用途。

所有字段都必须填写。管理器会自动生成内部 ID，不需要手工处理。

### 12.4 排序和删除

- 分组右侧的 `↑`、`↓` 调整分组顺序；
- 单个链接右侧的 `↑`、`↓` 调整组内顺序；
- `Delete` 删除对应分组或链接；
- 所有调整都只保存在当前编辑器中，选择 `Save Changes` 后才会写入 KV；
- 删除分组会同时删除该组内的所有链接，操作前建议先导出 JSON 备份。

### 12.5 JSON 导入、导出和备份

选择 `Export JSON` 会下载当前完整目录，文件中包含分组、链接和说明，不包含管理 Token。建议在大量调整前先导出一份备份。

选择 `Import JSON` 可以批量载入备份或手工整理的数据。导入后必须检查页面并再次选择 `Save Changes`，否则不会发布。

JSON 基本格式：

```json
{
  "version": 1,
  "updatedAt": null,
  "groups": [
    {
      "id": "daily-tools",
      "title": "Daily Tools",
      "description": "Services used every day.",
      "links": [
        {
          "id": "cloudflare-dashboard",
          "title": "Cloudflare Dashboard",
          "href": "https://dash.cloudflare.com/",
          "label": "Service",
          "description": "Manage domains and Workers."
        }
      ]
    }
  ]
}
```

手工修改 JSON 时，分组和链接的 `id` 只能使用小写英文字母、数字和连字符，并且不能重复。当前限制为最多 30 个分组、300 个链接和 256 KiB 请求体；只接受 HTTP 与 HTTPS 地址。

### 12.6 常见问题

#### 管理页提示 Token 无效

1. 确认访问的是 `www.youngkx.cn` 的正式管理页；
2. 运行 `npm.cmd exec wrangler -- whoami`，确认是正确账号；
3. 运行 `npm.cmd exec wrangler -- secret list`，确认存在 `LINKS_ADMIN_TOKEN`；
4. 重新复制 Token，避免首尾空格；
5. 如果 Token 已丢失，运行 `npm.cmd run links:secret` 设置新 Token。

Token 只保存在当前浏览器标签页的 `sessionStorage` 中。关闭标签页后再次进入管理页，需要重新输入，这是正常的安全行为。

#### 本地保存后线上没有变化

本地 `preview:test` 使用本地 KV，和 Cloudflare 线上 KV 分离。请进入正式管理页重新保存，或者在线上导入本地导出的 JSON。

#### 显示保存成功但公开页面暂时没有变化

先强制刷新 `/links/`。Cloudflare KV 在不同地区之间同步可能有短暂延迟，等待片刻后再次刷新即可；不要连续重复保存。

#### 导入 JSON 后无法保存

检查错误提示，并重点确认：

- 每个必填字段都不是空字符串；
- URL 以 `http://` 或 `https://` 开头；
- 所有分组 ID 和链接 ID 唯一；
- 文件小于 256 KiB；
- `version` 为数字 `1`。

---

## 13. 域名与重定向

Cloudflare 配置位于：

```text
wrangler.jsonc
```

当前绑定：

```text
youngkx.cn
www.youngkx.cn
```

Worker 代码位于：

```text
cloudflare/worker.ts
```

当前逻辑会把：

```text
https://youngkx.cn/任意路径?任意参数
```

永久重定向到：

```text
https://www.youngkx.cn/相同路径?相同参数
```

不要把 `www.youngkx.cn` 再重定向回 `youngkx.cn`，否则会出现无限循环。

Workers.dev 预览域名会由 Worker 添加 `X-Robots-Tag: noindex`，避免和正式域名形成重复收录；正式自定义域名不添加该响应头。`wrangler.jsonc` 还启用了 1% 采样的生产日志和链路追踪，排查线上问题时可在 Cloudflare 控制台查看。

### 13.1 Cloudflare DNS

当前域名使用 Cloudflare DNS。除非迁移服务商，不要随意更换域名服务器。

当前 Cloudflare Nameserver：

```text
anahi.ns.cloudflare.com
zeus.ns.cloudflare.com
```

如果域名突然无法访问，依次检查：

1. 域名是否到期；
2. Nameserver 是否仍是上面两项；
3. Cloudflare 中域名状态是否为 Active；
4. Worker `youngkxblog` 是否仍绑定两个自定义域名；
5. 最近一次部署是否成功。

---

## 14. 正式构建和部署

### 14.1 部署前检查

```powershell
npm.cmd run format:check
npm.cmd run validate
npm.cmd run test:e2e
npm.cmd exec wrangler -- secret list
```

其中 `validate` 已包含类型检查、代码规范、单元测试、生产构建与性能预算；不要在它失败时继续部署。

`secret list` 的结果必须包含 `LINKS_ADMIN_TOKEN`。如果不存在，先执行 `npm.cmd run links:secret`，否则线上 Links 管理页无法登录。

构建成功时会在终端列出：

- `/`；
- `/articles`；
- `/categories`；
- `/links`；
- `/links/manage`；
- 每篇文章地址；
- `/icon.png`；
- `/apple-icon.png`。

如果缺少文章或 Links 路由，不要部署，先检查构建输出和对应页面入口。

### 14.2 Cloudflare 部署预检

```powershell
npm.cmd run deploy:dry
```

预检不会修改正式网站。它应显示：

- Worker 名称为 `youngkxblog`；
- 静态目录为 `out`；
- 存在 `env.ASSETS` 绑定；
- 存在 `env.LINKS` KV 绑定。

### 14.3 正式部署

推送 `main` 后，Cloudflare 会自动构建并部署。如果需要立即手工发布，或自动部署暂时不可用，可以运行：

```powershell
npm.cmd run deploy
```

部署成功后会显示：

- Workers.dev 预览地址；
- `youngkx.cn`；
- `www.youngkx.cn`；
- 新的 Version ID。

请保存终端中的 Version ID，发生故障时可用于确认和回滚。

如果手工部署后 Cloudflare 自动构建又完成了一次，后完成的版本会接管流量。使用以下命令区分“已经上传的版本”和“当前正在承载流量的部署”：

```powershell
npm.cmd exec wrangler -- versions list
npm.cmd exec wrangler -- deployments list
```

线上验收应以 `deployments list` 中最新的 100% 流量版本为准，并继续检查正式域名实际加载的首页资源，而不是只记录第一次部署命令返回的 Version ID。

### 14.4 部署后验收

逐项打开并检查：

1. <https://youngkx.cn> 是否跳转到 `www`；
2. <https://www.youngkx.cn> 是否正常；
3. <https://www.youngkx.cn/articles/>；
4. <https://www.youngkx.cn/categories/>；
5. <https://www.youngkx.cn/links/>；
6. <https://www.youngkx.cn/links/manage/> 是否要求输入线上 Token；
7. 使用 Token 登录管理页，保存一份无内容变化的目录并确认成功；
8. 至少打开一篇文章；
9. 浏览器标签是否显示头像图标；
10. 亮暗主题切换；
11. 手机导航菜单；
12. 文章目录、复制链接和返回顶部；
13. 随便输入一个不存在的地址，确认返回 404。

---

## 15. GitHub 提交规范

### 15.1 推荐方式：使用分支

```powershell
git pull --ff-only
git switch -c update/本次修改名称

# 修改并测试后
git status
git diff
git add 明确的文件名
git commit -m "说明本次修改"
git push -u origin update/本次修改名称
```

随后在 GitHub 创建 Pull Request，确认无误后合并到 `main`。

### 15.2 提交信息示例

```text
Add a new Web article
Update homepage copy
Fix mobile navigation spacing
Refresh site icons
Update Cloudflare redirect
```

### 15.3 不要提交的内容

- `node_modules/`；
- `.next/`；
- `out/`；
- `.wrangler/`；
- `.env` 或 `.env.local`；
- Cloudflare/GitHub Token；
- 临时日志和截图；
- 与本次修改无关的大量文件。

---

## 16. 回滚方法

### 16.1 代码尚未提交

先运行：

```powershell
git status
git diff
```

确认只是误改后，再使用编辑器撤销。不要在不清楚影响时执行 `git reset --hard`。

### 16.2 已提交但尚未部署

推荐创建一个反向提交：

```powershell
git log --oneline
git revert 提交编号
git push
```

`git revert` 会保留完整历史，比强制覆盖主分支安全。

### 16.3 已部署到 Cloudflare

有两种方式：

1. 在 Cloudflare 控制台的 Worker 部署记录中选择上一个正常版本并回滚；
2. 在 GitHub 对错误提交执行 `git revert`，重新运行构建和 `wrangler deploy`。

回滚后重新检查根域名、`www`、文章、分类和 404。

---

## 17. 常见问题

### 17.1 `npm.ps1 cannot be loaded`

原因：PowerShell 禁止运行脚本。

解决：

```powershell
npm.cmd run dev
npm.cmd run build
```

### 17.2 `wrangler` 未登录

```powershell
npx.cmd wrangler login
```

完成浏览器授权后重新部署。

### 17.3 构建成功但线上没有变化

常见原因：只执行了 `git push`，没有部署 Cloudflare。

```powershell
npm.cmd run build
npx.cmd wrangler deploy
```

### 17.4 页面显示旧内容

1. 强制刷新；
2. 使用无痕窗口；
3. 检查 Cloudflare 最新 Version ID；
4. 确认部署命令是在正确项目目录执行；
5. 确认 `out/` 是本次修改后生成的。

### 17.5 文章页面 404

检查：

- `content/articles.json` 的 `key`；
- `HomePage.tsx` 中的 `href`；
- 中文标点和大小写；
- `path` 是否包含年份、月份、日期和文章别名；
- `npm.cmd run build` 输出中是否出现该文章。

### 17.6 根域名不跳转

检查：

- `cloudflare/worker.ts` 是否仍包含 `youngkx.cn` 判断；
- `wrangler.jsonc` 是否包含两个自定义域名；
- 是否执行了最新的 `wrangler deploy`；
- DNS 是否仍由 Cloudflare 托管。

### 17.7 网站图标没有更新

检查：

- `app/icon.png` 是否存在；
- `app/apple-icon.png` 是否存在；
- `npm run build` 输出中是否包含图标路由；
- 是否重新部署；
- 浏览器是否仍在使用旧缓存。

### 17.8 中文乱码

所有源码、JSON 和 Markdown 文件都必须保存为 UTF-8。不要使用 ANSI 或 GBK 保存。

---

## 18. 每次发布的最终检查清单

发布前：

- [ ] 修改内容已经在本地确认；
- [ ] 中文没有乱码；
- [ ] 所有文章标题、日期和链接一致；
- [ ] 主页只展示预期的三篇文章；
- [ ] 分类数量和文章总数正确；
- [ ] 页脚最近更新时间已经修改；
- [ ] 亮色和暗色主题正常；
- [ ] 手机端排版正常；
- [ ] 首屏标题随滚动平滑退出，简介与操作入口自然展开；
- [ ] 顶栏 `Cards` 点击和带锚点刷新均落在正确位置，标题留白及字号与 Articles、Topics 一致；
- [ ] 高速下滑会先停在完整的四卡片动态索引区；
- [ ] 卡片整体旋转展开和单卡鼠标 3D 倾斜正常，翘起部分没有被包装层裁切；
- [ ] 亮暗主题下四张卡片之间和周围没有额外底板、边框、背景色或外层阴影；
- [ ] 鼠标移动、点击和触摸粒子效果正常；
- [ ] 主题切换和跨页面跳转的粒子过渡自然且不会遮挡交互；
- [ ] “减少动态效果”模式不会播放首屏展开和粒子交互；
- [ ] `/links/` 和 `/links/manage/` 在本地预览中正常；
- [ ] `LINKS_ADMIN_TOKEN` 已在正确的 Cloudflare Worker 中配置；
- [ ] `npm.cmd run format:check` 通过；
- [ ] `npm.cmd run validate` 通过；
- [ ] `npm.cmd run test:e2e` 通过；
- [ ] 性能预算没有被无说明地放宽；
- [ ] `git diff` 中没有密码、Token 或无关文件。

发布后：

- [ ] GitHub `main` 已包含本次修改；
- [ ] Cloudflare 部署成功，并已用 `wrangler deployments list` 确认最新版本承载 100% 流量；
- [ ] 根域名正确跳转到 `www`；
- [ ] 主页、文章页、分类页和 Links 页均为 200；
- [ ] Links 管理页可以用生产 Token 登录并保存；
- [ ] 随机文章可打开；
- [ ] favicon 正常显示；
- [ ] 不存在的地址返回 404；
- [ ] 复制链接、目录、主题切换和返回顶部正常。

---

## 19. 最重要的维护原则

1. 修改前先拉取最新代码；
2. 一次只处理一类修改；
3. 不要直接编辑 `out/`；
4. 不要提交账号密码或 Token；
5. 不要恢复旧 Hexo、GitHub Pages 或旧静态服务器文件；
6. 每次部署前必须执行类型检查和生产构建；
7. `main` 推送会触发 Cloudflare 自动部署；手工部署后仍要确认后续自动版本是否接管流量；
8. 删除文件前先确认它不是当前 Next.js 或 Cloudflare 配置的一部分；
9. 重要改动使用 Git 分支和 Pull Request；
10. 出现问题优先回滚，不要在正式站点上连续试错。
